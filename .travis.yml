matrix:
  include:
    - language: python
      dist: xenial
      python: 3.5
      before_install:
        # Workaround https://github.com/travis-ci/travis-ci/issues/7940
        - sudo rm -f /etc/boto.cfg
      install:
        - sudo apt install openjdk-8-jdk
        - sudo pip install pytz==2018.7
        - sudo pip install -r requirements.txt
      script:
        - make test
